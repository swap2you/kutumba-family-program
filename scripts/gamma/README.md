# Gamma API (optional — not required for v3.0 pass)

Gamma may be used manually via Create with Agent, Paste, or Import.

## API (future)

- Requires Gamma Pro or higher
- Keep `GAMMA_API_KEY` in local `.env` only — never commit `sk-gamma-*`
- Header: `X-API-KEY`
- No executable API calls in this repository phase until explicitly enabled

See module `gamma/GAMMA-*-DECK-PROMPT.md` files for card content.
