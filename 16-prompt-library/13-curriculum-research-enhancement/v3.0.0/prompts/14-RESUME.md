# Prompt 14 — Resume an Interrupted Enhancement Run

Read:

1. `CURRENT-STATUS.md`
2. `build-evidence/CURRICULUM-ENHANCEMENT-LEDGER.md`
3. `build-evidence/WEEK-QUALITY-DASHBOARD.md`
4. latest cycle audit
5. latest validation report
6. `git status`
7. recent Git log

Trust repository evidence over prior chat summaries.

Do not restart completed modules.

Resume from the first module or phase whose required acceptance criteria are incomplete.

Before editing, update the ledger with:

- resume timestamp;
- current HEAD;
- dirty files;
- current module;
- last passing validation;
- next exact action.

Prevent duplicate research folders, duplicate claims, duplicate visuals, and duplicate source entries.
