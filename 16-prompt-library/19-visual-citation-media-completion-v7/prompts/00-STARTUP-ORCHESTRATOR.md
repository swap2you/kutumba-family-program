# Prompt 00 — Startup Orchestrator

You are the execution orchestrator for KUTUMBA V7.

## Repository

Work only in:

`C:\Development\Workspace\DevotionalRepo\kutumba-family-program`

Remote:

`https://github.com/swap2you/kutumba-family-program.git`

Branch:

`main`

The repository must remain PUBLIC.

## First commands

Run and record:

```powershell
git status --short
git branch --show-current
git rev-parse HEAD
git log --oneline --decorate -25
git remote -v
gh repo view swap2you/kutumba-family-program --json visibility,isPrivate,url,defaultBranchRef
```

Stop only when:

- the path is not the Git root;
- the branch is not `main`;
- uncommitted user work exists that cannot be safely separated;
- the remote is not the expected repository.

Do not stop for ordinary fixable validation failures.

## Read before editing

Read completely:

- `AGENTS.md`
- `README.md`
- `CURRENT-STATUS.md`
- `ROADMAP.md`
- `00-foundation/LOCKED-BASELINE-REGISTER.yaml`
- `RIGHTS-AND-USE.md`
- `SECURITY-PRIVACY.md`
- `14-research-source-register/SOURCE-AUTHORITY-POLICY.md`
- `build-evidence/V6-FINAL-CONTENT-TRUTH-REPORT.md`
- `17-reviews-and-audits/V6-INDEPENDENT-PILOT-READINESS-AUDIT.md`
- every prompt in this V7 library

## Execution mode

Execute Prompts 01–10 in order.

Do not ask the user to approve ordinary intermediate work. Continue through all phases unless a true blocker is encountered.

Use logical commits after each completed phase. Push only after validation passes for that phase. Never force-push.

Do not perform blind repository-wide rewriting. Every modified curriculum file must be traceable to:

- a verified factual correction;
- a visual production requirement;
- a source/media mapping improvement;
- a validator defect;
- an explicit V7 deliverable.

## Required progress files

Maintain:

- `build-evidence/V7-EXECUTION-LEDGER.md`
- `build-evidence/V7-FINDINGS-REGISTER.yaml`
- `build-evidence/V7-FILE-CHANGE-MANIFEST.csv`

For each changed file record:

- path;
- reason;
- finding ID;
- source/evidence;
- old status;
- new status;
- review still required;
- commit SHA.

## Final response

Return only after Prompt 10 is complete:

- actual final HEAD;
- commits pushed;
- files added/changed;
- facts corrected;
- visuals created and rendered;
- rights status;
- media entries curated;
- Gamma readiness;
- kathā runtime correction;
- validation output;
- human gates open;
- Workstream 9 and Setu status;
- publication verdict;
- clean working-tree proof.
